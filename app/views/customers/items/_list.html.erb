<div class="row">
	<% items.each do |item| %>
		<div class="col-lg-3">
			<%= link_to item_path(item.id) do %>
				<%= attachment_image_tag item, :image, format: 'jpeg', size: "200x150", fallback: "no_image.jpg" %>
			<% end %>

			<ul style="list-style: none; margin:10px 0; padding: 0;">
				<li><%= link_to(item.name, item_path(item.id)) %></li>
				<li>¥<%= (item.price * 1.1).to_i.to_s(:delimited) %></li>
			</ul>
		</div>
	<% end %>
</div>
